@using Web.Html.Input
<!-- Locator bar -->
@using (var row = Html.BeginRowContainer())
{
    using (var col = Html.BeginColumnContainer(6))
     {
        @Html.Partial("RecentCities")
     }
    using (var col = Html.BeginColumnContainer(6))
    {
        @Html.Partial("SearchBox")
    }
}
