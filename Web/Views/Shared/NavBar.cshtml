@using Web.Extensions
<nav class="navbar navbar-default navbar-static-top">
    <div class="container">
        <!-- title -->
        <div class="navbar-header pull-left">
            @Html.ActionLink("WeatherOrNot", "Index", "Home", new { area = "" }, new { @class = "navbar-brand" })
        </div>


        <!-- these items are sticky and don't collapse-->
        <div class="navbar-header pull-right">
            @if (ViewContext.IsHomePage())
            {
                <ul class="nav pull-left">
                    <li class="pull-left">@Html.Partial("AutoUpdateIndicator")</li>
                    <li class="pull-left">@Html.Partial("SearchControl")</li>
                </ul>
            }

            <!-- hamburger button -->
            <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
        </div>

        <div class="navbar-collapse collapse navbar-left">
            <ul class="nav navbar-nav pull-right">
                <li>@Html.ActionLink("About", "About", "Home")</li>
                <li>@Html.ActionLink("Contact", "Contact", "Home")</li>
            </ul>
            @*@Html.Partial("_LoginPartial")*@
        </div>

        <!-- Additional navbar items
        <div class="collapse navbar-collapse navbar-right">
            <ul class="nav navbar-nav pull-right">
                <li><a href="/locator">Locator</a></li>
                <li><a href="/extras">Extras</a></li>
            </ul>
        </div>-->

    </div>
</nav>

